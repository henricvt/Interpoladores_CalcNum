# Esta é uma versão básica de um algoritmo interpolador de Newton. No momento, sua aplicação está restrita a interpolação para apenas um determinado valor a ser escolhido.
# Uma forma de tornar esta aplicação mais robusta seria por meio da implementação de alguma(s) função(es) que armazene(m) uma quantidade razoável de valores interpolados a serem plotados via MatPlotLib, por exemplo.

